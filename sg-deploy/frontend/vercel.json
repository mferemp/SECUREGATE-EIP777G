{
  "buildCommand": "npm run build:client",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
